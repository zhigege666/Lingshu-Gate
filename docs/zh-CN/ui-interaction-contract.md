# UI 交互约束

[English](../ui-interaction-contract.md) · [文档索引](README.md)

本文是新增或迁移 Console UI 及其受影响消费者的验收契约，适用于全部 Console 页面，包括工具、角色权限和调用。它不表示当前全站已经实现这些要求，不要求重写无关页面，也不增加授权或审批流程。

目标是高效完成任务：找到对象、执行预期操作、理解结果并从失败中恢复。除非变更明确涉及相应行为，否则保留现有公共 API、权限、确认边界和请求语义。

## 现有基础

- 保留 React/Vite，以 Ant Design 为主组件基础；不为本约束引入另一套组件库。复用现有 Ajv 和领域校验适配器。专用编辑器或组件预览工具需要解决明确缺口，不是交互修复的前置条件。
- 扩展现有主题入口：[`console-design-provider.tsx`](../../web/src/components/console-design-provider.tsx)、[`index.css`](../../web/src/index.css) 和 [`console-workspace.css`](../../web/src/console-workspace.css) 中的布局规则。不新建竞争性的 token 入口。现有 AntD tokens 与 CSS 变量的收敛必须保护受影响消费者，并作为独立迁移进行。
- 数值以这些文件为依据，不把设计研究中的试验尺寸当作现行标准。例如，当前 AntD provider 定义了 `fontSize: 14`、`borderRadius: 6`、`controlHeight: 36`、`controlHeightSM: 30`；这些只描述 provider，不表示所有现有本地控件都与之相同。修改 token 必须明确提出并检查其消费者。
- 公共组件负责表现与交互机制；领域适配器负责请求、授权、业务状态和秘密处理。行为一致时才复用，不把外观相似的页面封成万能 CRUD 组件。

可编辑控件边界使用 `--input` / AntD `colorBorder`，包括调用页的可空值和仅 JSON 编辑值容器。结构分割线、表格、卡片和只读结果使用较弱的 `--border` / `colorBorderSecondary` / `colorSplit`。两种主题都要保持这些映射一致；焦点和错误指示仍使用各自的语义颜色。修改 token 后，按实际相邻背景检查渲染后的控件。构建注入的 Console 版本在桌面和移动端固定页眉中保持可见，较长的预发行版本截断时仍可获取完整值。

## 规则

以下规则是本项目的设计决定。参考来源说明其借鉴的方法，不代表整套引入来源项目的规则或框架。

| ID | 验收要求 |
|---|---|
| UI-01 · 页面身份 | 保留一个清晰、可访问的页面身份。允许紧凑或同行标题，保留现有桌面/手机的身份展示安排。不叠加装饰分类标签、同义大标题和重复介绍。必要对象名称和任务说明仍然可见。 |
| UI-02 · 有效空间 | 高度由内容决定。不为布局完整预留空列、等高空面板、占位统计或大等待区。保留必要分组间距和可用的编辑器/日志工作区。区分真正无数据、加载、错误、无权限和未选择状态。 |
| UI-03 · Tokens | 复用现有语义颜色、间距、字体、圆角和层级值。新增任意值应有具体布局理由并接受局部评审。`0`、`auto`、百分比和内容驱动尺寸不自动构成违规。不能暗示已存在 token lint。 |
| UI-04 · 密度 | 同一任务区控件密度一致；长文本和错误允许增加高度。紧凑布局在窄屏和缩放时仍须可读、可点击、操作可达。不为所有内容设置未经验证的统一固定行高。 |
| UI-05 · 表单 | 已知字段用有类型控件，支持的数组用可增删行，合适的映射用键值编辑。标签必须关联输入，placeholder 不替代标签。区分用途、格式/单位帮助和校验。默认单列，强相关短字段适合时可同行。 |
| UI-06 · 校验 | 错误结构包含代码、消息、严重程度、来源、文档路径和草稿版本；可定位文本时附范围。JSON 原文变化时即提供语法诊断；普通必填/格式错误在相应字段被操作后或尝试提交时展示，不在尚未操作的表单上堆满警告。可修复错误在字段旁持续可见，多条或隐藏字段错误提供摘要。摘要动作展开并聚焦字段或 JSON 位置。不只依赖 Tooltip 或临时 Toast。 |
| UI-07 · 表单/JSON | 两种模式自动同步同一语义草稿，不增加需要用户操作的同步/应用步骤，不丢失未展示数据。保留非法原文，绝不以之前的合法值代替提交。保留未知字段、数组顺序及缺失、`null`、`0`、`false`、空字符串的区别。修改区分设置、移除、保持。遵守下文编辑器边界。 |
| UI-08 · 反馈 | 字段错误在字段，区域加载/失败在相应区域，短成功提示用消息，持续任务结果放在任务上下文。持续失败必须有可见恢复入口。同一结果避免重复提示。防止重复提交，明确哪个提交快照拥有响应。 |
| UI-09 · 浮层 | 编辑、只读详情和危险确认分工明确。提供可访问名称、合适的初始焦点、键盘导航和合理的焦点返回。定义 dirty 和 pending 时的关闭行为。长表单操作可达且不遮挡最后字段。能就地完成任务时不新增打断式弹窗。 |
| UI-10 · 列表与表格 | 搜索范围、已应用筛选、加载范围、分页和选择范围真实明确。行主操作使用真正的链接或按钮。高频操作可发现；批量结果保留未解决项的选择并说明部分失败。限量快照不能暗示全服务总量。 |
| UI-11 · 空状态 | 说明真实状态，提供可用下一步，如创建、清除筛选或重试。请求失败不能显示成空集合。上下文已清楚时用紧凑提示，不再增加标题或插画。 |
| UI-12 · 日志与任务 | 区分快照、连接、执行和跟随滚动状态。新记录不能抢走阅读位置。保留来源的真实时间、范围和传输能力；快照不标成实时流。展示组件不能偷偷启动轮询或虚构取消能力。 |
| UI-13 · 可访问性 | 优先原生控件语义、可访问名称、可见焦点和非颜色信息。检查变更任务的键盘路径，包括菜单和浮层。自动可访问性结果不能替代焦点顺序、阅读顺序、缩放和任务完成检查。 |
| UI-14 · 文案 | 领域术语一致，动作标签具体；危险确认说明动作、目标和后果。维护中英文词条、单位和时间含义。诊断标识有助排查且不暴露秘密；原始日志不按界面文案强行翻译。 |
| UI-15 · 复用 | 新复合组件说明真实消费者、支持状态以及交给调用方的职责。显式迁移并检查受影响消费者。采用公共模式时保留工具、角色和调用的最新行为，不平行重建它们的新能力。 |

## 编辑器与领域边界

表单/JSON 转换作用于当前文档与版本。表单修改自动更新 JSON 表达，JSON 解析成功后自动更新语义草稿；切换视图不需要额外点击“应用 JSON”或“同步表单”。不能每次输入都格式化原文或移动光标。JSON 语法错误时保留原文、立即提供语法诊断并阻止提交，旧解析结果不能作为回退。切换视图不得丢弃非法原文或用之前的合法值替换它。

校验与错误展示时机分开处理。普通必填/格式错误在字段被操作后或尝试提交时展示，初始空表单不列出全部缺失字段。用户修正已展示错误的字段时重新校验。任何写入或执行前，提交始终校验完整当前草稿，包括未操作和隐藏的字段。必填错误需要完整路径，包括缺失属性；摘要动作展开并聚焦隐藏字段或对应 JSON 位置。原文语法错误立即展示，因为它阻碍对当前文档的可靠解释。

Schema 变化、迟到响应和刷新不得覆盖 dirty 草稿。参数撤销不能恢复旧执行结果。本地同步规则不合并领域中原本独立的保存/应用或确认/发布决定。

键值行中点击“添加”前已经输入的内容也属于草稿，包括只填了键名、尚未填值的情况。它必须参与 dirty 判断和离开保护。保存、运行、切换模式、替换参数或移除其父项时，不能静默忽略或丢弃这些输入：应安全保留，或提示用户先添加或明确清除，并能定位到该行。行级“添加”不代表额外的表单/JSON 同步步骤。

所有会替换编辑上下文的入口采用同一离开决定：弹窗关闭、应用导航、命令导航以及浏览器前进/后退。dirty 表单提供继续编辑或明确放弃；表单提交期间保持编辑器挂载。调用页仍使用其独立提示，说明离开后请求可能继续执行。浏览器文档退出在支持时使用 `beforeunload`，不承诺防护浏览器崩溃。共享退出注册表只保存 dirty/pending 标记，不保存字段值或秘密。

表单只能展示能够安全表达的结构。不支持的 Schema 方言、引用或构造不能被当作校验成功。沿用现有失败关闭路径，给出可处理原因，或在支持时保留 JSON 编辑。JSON 模式不能绕过校验。解析、重复键检测、数字精度保护、结构校验和领域检查是不同能力；不能声称 `JSON.parse` 或 Ajv 自动具备全部能力。

工具参数依工具 Schema。Manifest 编辑依实际项目模型和现有检查；请求中的通用 `dict` Schema 不等于完整 Manifest 表单规范。只读审计、日志和结果 JSON 不因此获得编辑权限。共享编辑器结构不能暗中增加预检请求或改变保存/应用分离行为。

| 领域 | 必须保留的边界 |
|---|---|
| 已有配置的脱敏 endpoint | 显式区分保持/替换。保持脱敏 endpoint 时沿用绑定原配置 ID 的现有保留协议，不把掩码当真实 URL 校验，不回填秘密地址，不取消后端脱敏。替换值必须正常通过 endpoint 校验。新建配置不得接受掩码作为有效 endpoint。该领域保持状态不放宽 UI-07 或 Schema 失败关闭规则。 |
| Manifest 一次性凭据 | `user_credential_values` 不进入可往返 Manifest 和普通撤销/复制/历史。保留提交前移出编辑状态、失败后重新输入的现有语义。 |
| 全局凭据 | 保留失败时现有受控输入生命周期和领域保持/替换语义。不能将掩码当新值提交，也不能由通用错误处理清除所有秘密。 |
| 下游凭据 | 不查询或预填旧秘密。保留成功/关闭时清空以及当前尝试的失败处理行为。 |
| 个人 Token | 完整值仍为仅创建后展示一次的结果，与创建草稿分离；不暗示后续可以重新查询全文。 |
| 临时密码与工具参数 | 保留各自提交/会话生命周期。不能从 Manifest 推导清空或持久化规则。秘密不得进入 URL，也不得自动持久化到浏览器存储。 |
| 执行与导航 | 离开调用页不能被称为取消。按真实会话生命周期定义运行中或实际未保存修改的提醒；不静默重试，不将敏感草稿/结果提升为持久化历史。 |
| 决定与权限 | 保留现有上传、构建、部署、覆盖、启动、取消、放弃、保存/应用、分类确认/发布以及 Token 扩权的独立决定。发现和展示不授予访问权。 |

本地与远端筛选须说明行为。远端查询区分筛选草稿和已应用条件，显示尚未应用的变化；拟定的“重置条件”只重置草稿，直到使用“应用筛选”。本地筛选可以即时作用于已载入记录。这是迁移目标，不表示当前每页已遵守。刷新明确作用范围，不能暗中请求所有页面数据。

## 已迁移共享模式与消费者

下表列出当前源码已接入的消费者，不表示全部状态已经通过浏览器或独立评审。领域确认、权限、请求归属和秘密生命周期仍由调用方负责。

| 模式 | 实际消费者 | 支持行为与调用方职责 |
|---|---|---|
| [`FormDialog`](../../web/src/components/form-dialog.tsx) | 9 页共 12 个编辑实例：角色权限（角色/权限类型）、用户（创建/编辑）、工具分类（批量/审核）、配置、上传、全局凭据、下游凭据、资源授权、个人 Token | 滚动字段区外固定头部/操作区；持续表单错误；pending 关闭锁定；可访问标题和说明；注册 dirty/pending。字段、校验、提交状态、真实 dirty 比较、关闭/放弃和提交行为由调用方提供。只读详情和危险确认继续使用独立组件。 |
| [`EditorNavigationContext`](../../web/src/components/editor-navigation-guard.tsx) 与 [`useDraftCloseGuard`](../../web/src/components/use-draft-close-guard.ts) | 退出注册覆盖上述 12 个 `FormDialog` 实例；关闭 helper 供采用它的编辑器使用 | 每个已打开编辑器独立注册和清理退出标记；采用局部关闭 helper 的编辑器防止重复放弃提示，角色、配置和上传保留自身领域关闭处理。App 决定导航，调用方负责清理和秘密处理。调用页使用自身会话退出状态；一次性 Token 结果不视为未保存表单草稿。 |
| [`ValidationErrors`](../../web/src/components/validation-errors.tsx) | 配置/上传中的 `McpConfigEditor`，以及调用参数编辑器 | 持续错误摘要，包含代码/来源/路径/版本及定位操作。适配器产生诊断、选择展示时机、排除过期版本，并展开/聚焦相应字段或 JSON 位置。其他有类型表单保留字段校验和持续服务器错误。 |
| [`McpConfigEditor`](../../web/src/components/mcp-config-editor.tsx) | 配置；上传 → 另存为配置 | 共用 Manifest 表单/JSON、受支持的参数行和键值映射、待添加项保护、脱敏 endpoint 保持/替换，并配合调用方的一次性凭据提交边界。调用方提供原配置身份、保存请求和秘密生命周期。配置保留预检；上传关闭后端预检与凭据列表加载，保留独立创建请求。不支持的结构留在 JSON。 |
| [`PageRefreshContext` / `usePageRefresh`](../../web/src/components/page-refresh.tsx) | 13 个页面 loader：构建、全局凭据、用户、角色、授权、工具分类、个人 Token、下游凭据、调用审计、日志事件、运行缓存、上传、诊断。App 为仪表盘、配置、服务、工具、调用提供限定范围的读取。 | 壳调用当前挂载页面的读取函数并尊重其忙碌状态。必要依赖和错误恢复由调用方定义；刷新不重挂编辑器、不轮询、不应用尚未提交的筛选，也不请求全站数据。刷新日志等明确局部动作可保留。 |
| [`QueryStatus`](../../web/src/components/query-status.tsx) | 日志事件、调用审计 | 展示未应用变化、已应用条件和成功快照时间。页面负责筛选草稿/已应用条件、限量、请求和错误；“重置条件”只改变下一次查询。本地搜索仍仅针对已加载记录。 |
| [`getToolAccessDisplay`](../../web/src/features/tool-access.ts) | 工具目录（经 `tool-catalog`）与调用 | 两处均从 `metadata.gate_access.required_access` 得到展示级别；仅明确 pending 分类显示待审核，unknown 保持独立。此适配器不从声明推断访问权，也不授予执行权限。 |

## 证据与评审

按变更行为和风险选择适用状态及检查，不要求状态、语言、主题、窗口尺寸的全排列。相关样例包括加载、刷新保留旧数据、无匹配、失败、无权限、dirty、提交中、完成、长内容和窄屏。主题/文案/布局变更需要相应亮暗主题、中英文或缩放证据。

| 层次 | 证明什么 | 当前状态与限制 |
|---|---|---|
| 1. 源码与架构检查 | TypeScript 与现有源码/语言词条契约；新增导入或架构规则须显式实现 | `npm --prefix web run check` 执行类型检查与 `check:ux`。语言词条经 TypeScript AST 检查：两个必需语言对象必须存在、非空，且没有重复或缺失键。其他 UX 规则仍检查源码字符串，包括 `PageHeader` 和帮助入口。在专项变更替换之前保留这些门禁；它们不能证明渲染行为。 |
| 2. 组件行为 | 可观察状态变化、无损转换、请求归属和错误恢复 | 使用相关现有 Vitest 测试，并针对变更行为补充必要测试。测试文件存在不代表已执行。 |
| 3. 浏览器证据 | 真实布局、滚动、焦点、溢出和键盘操作 | 捕获适用页面/状态。标明模拟数据，与真实 API/权限验证区分。本文没有将 token lint、Playwright、axe 和 Storybook 建立为仓库现有门禁。 |
| 4. 独立 UI 评审 | 任务清晰、密度合理、交互一致、领域边界保留 | 由独立于实现/方案作者的评审者检查方案和证据、记录问题并复核修正。该评审不产生新的用户权限确认。 |

记录命令/场景、实际结果、范围及未执行项。数据丢失、秘密暴露或危险误执行阻断验收。主要任务键盘不可用、持续错误被隐藏或操作范围误导需要修正。外观问题可记录受影响范围和下一步。源码检查或截图一致都不能单独证明全站 UI 通过。

纯文档变更检查链接、语言一致性和 `git diff --check`。产品代码遵循根目录说明，运行相关现有检查，包括 `npm --prefix web run check`、`npm --prefix web test` 和 `npm --prefix web run build`；实际结果与浏览器、独立评审证据分别报告。

## 落实状态与后续工作

1. 已在 [`i18n-contract.mjs`](../../web/scripts/i18n-contract.mjs) 完成词条提取修复，由 `check:ux` 调用。使用现有 TypeScript AST 读取顶层 `messages` 对象，兼容格式、语言顺序和 `as const`/`satisfies` 变化；语言对象缺失/为空、属性重复、键不一致、语法错误或无法可靠分析的动态成员均失败。不执行应用源码。回归用例包含真实词条文件，运行 `npm --prefix web test -- scripts/i18n-contract.test.mjs`；它验证静态词条契约，不验证翻译含义或浏览器行为。
2. 用独立专项变更将源码字符串门禁逐步改为可靠语法/数据检查和浏览器行为证据。保留有价值的分类、fingerprint、批量约束。在页面身份和必要帮助有替代覆盖前，不削弱 `PageHeader` 或帮助入口门禁。
3. 上述消费者已迁移到共享编辑器、反馈、查询、刷新和退出模式。继续完成并记录适用浏览器检查、独立产品/UI 与测试评审，修正发现的问题并复查受影响消费者。组件已接入或源码测试通过，均不等于全站交互验收通过。
4. 在现有入口梳理 AntD tokens 和 CSS 变量，再通过消费者检查消除已确认差异。仅在试点发现具体、反复出现的需要时引入 lint、组件样例或浏览器工具，并写清实际安装和执行的范围。

## 开源参考

以下一手来源为本约束提供方法参考；上面的项目专属规则仍是明确的项目决定。借鉴评审方式不要求采用来源的组件库或完整测试矩阵。

- [Primer 贡献规则](https://github.com/primer/react/blob/main/contributor-docs/CONTRIBUTING.md)与[测试策略](https://github.com/primer/react/blob/main/contributor-docs/testing.md)：真实复用、主题值、API、状态/行为覆盖和评审证据。
- [Carbon 组件清单](https://carbondesignsystem.com/contributing/component-checklist/)、[token Stylelint 插件](https://github.com/carbon-design-system/stylelint-plugin-carbon-tokens)及[表格指南](https://carbondesignsystem.com/components/data-table/usage/)：token 治理、行为规格、渐进检查和密度变体。
- [GitLab Pajamas 表单](https://design.gitlab.com/patterns/forms/)、[弹窗指南](https://design.gitlab.com/components/modal/)及[空状态](https://design.gitlab.com/patterns/empty-states/)：关联的帮助/错误、焦点与关闭行为、符合上下文的下一步。Pajamas 是托管于 GitLab 的开源项目。
- [Ant Design 反馈](https://ant.design/docs/spec/research-message-and-feedback/)、[主题定制](https://ant.design/docs/react/customize-theme/)及[视觉回归实践](https://ant.design/docs/blog/visual-regression/)：场景化反馈、现有 token 机制和视觉变更证据。
