# 安全策略

[English](SECURITY.md)

Lingshu Gate 位于认证用户与可执行下游服务之间。其配置、数据目录、项目交付输入和管理员账户都属于安全敏感资产。

## 报告漏洞

请通过仓库的 **Security** 页面选择 **Report a vulnerability**，以私有 Advisory 报告。不要在公开 Issue 或 Pull Request 中披露利用细节、凭据、私有日志或尚未修复的漏洞。

在条件允许时请提供：

- 受影响的版本、Commit、操作系统和部署方式；
- 已移除所有 Secret 的最小复现请求或配置；
- 预期与实际的授权边界；
- 影响、所需权限，以及是否发生代码执行或 Secret 泄露；
- 建议的缓解方式或补丁。

如果无法使用私有报告，请创建一个不含漏洞细节的 Issue，请维护者提供私有沟通渠道。维护者会确认完整报告、调查问题、协调修复，并在用户具备可执行修复方案时发布 Advisory。

## 支持范围

安全修复面向最新公开发行版和 `main` 分支。旧版可能必须升级才能获得修复。发行归档和容器镜像应从仓库官方 Releases 与 Packages 页面获取，并按摘要完成校验。

## 部署边界

- 服务直接绑定回环地址。远程访问应置于 HTTPS 反向代理之后，并且只暴露必需路由。
- 保持认证开启。立即修改一次性管理员密码，并使用独立的最小权限账户。
- 公共 Origin 使用 HTTPS 时，设置 `LINGSHU_GATE_AUTH_COOKIE_SECURE=true`。
- 把 `LINGSHU_GATE_TRUSTED_PROXY_IPS` 设为精确代理地址或最小内部 CIDR；禁止在不受控网络设为 `*`；代理必须覆盖客户端传入的转发头。
- 把 `LINGSHU_GATE_MCP_ALLOWED_ORIGINS` 设为允许调用 `/mcp` 的精确浏览器 Origin；Gate 会对其他所有显式 `Origin` 返回 HTTP 403。
- 保护 `/data` 与 `/config`，定期备份，并确保只有 Gate 服务账户可写。凭据密文和本地密钥必须作为整体保护。
- 默认保持 workspace 只读；只有明确记录的操作才能获得更窄范围的可写路径。
- 每个 SQLite 数据库只运行一个 Core 副本，不得让多个 Gate 实例并发共享 SQLite volume。
- 保持 payload 日志关闭；分享日志和审计导出前先复核内容。
- 生产容器镜像固定到 digest，并校验发行 checksum、SBOM 和构建元数据。

## 执行边界

项目构建和受管本机进程会以 Gate 进程的权限执行代码，不能作为不可信源码的沙箱。

- 只构建和启动完整源码及依赖行为都可信的项目。
- 上传前复核确定性压缩包中的完整文件列表。
- 确认前复核准确的构建计划和需要访问网络的依赖安装步骤。
- 不要在项目归档、Manifest、工具参数、日志或构建命令中传递 Secret。
- 将部署、覆盖、启动、取消和放弃会话视为不同的写操作。
- 不要向 Core 服务挂载容器引擎 Socket。
- 原生受管容器目标只有在管理员复核镜像 Digest、Mount、Network 和资源限制后，才能使用本机容器引擎。

Docker Core 镜像不会启动本机 stdio 进程或执行项目构建。这些操作应在单独受控的原生环境中完成。

## 访问控制边界

最终工具访问权限是认证状态、控制权限、资源授权、已发布的读写分类和 API Token scope 的交集。Tool annotation 和发现到的 Schema 都是不可信提示，不能自行授予权限。

管理员应：

- 只在人工复核后分类并发布工具；
- 授予最小的服务与工具资源范围；
- 在可行时使用短期且 scope 最小的 API Token；
- 检查调用审计和授权拒绝；
- 工具刷新后重新复核新增、变化、消失或重新出现的工具。

## 凭据边界

系统凭据和用户下游绑定会加密保存，并在 API 响应中以掩码显示。加密不能替代主机访问控制：能够同时读取加密存储和密钥材料的主体可以恢复明文。

Manifest 应使用 `${credential:<id>}` 引用而不是明文。用户下游值只注入该用户的独立 HTTP 请求上下文；Gate 会拒绝下游 HTTP Redirect，避免这些 Header 被转发到其他端点。不要为共享 stdio 进程配置用户专属 Secret。

备份时同时保留密钥材料和密文，对备份采用同等保护，并在不打印 Secret 的前提下测试恢复。

## 不在范围内

如果问题只存在于某个下游服务，应报告给该服务的维护者；除非它同时证明 Gate 违反了文档约定的隔离、授权、脱敏或生命周期边界。
