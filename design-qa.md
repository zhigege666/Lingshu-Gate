# MCP 服务工作区验收

日期：2026-09-15。范围：统一控制台主题与导航，首批 MCP 服务列表、服务详情及工具发现性能。

## 结论

当前没有待修复的 P0/P1/P2 视觉问题。实现采用 Ant Design，保留既有 React、路由、权限和服务操作能力。其余业务页面继承公共主题与导航，尚未逐页重做。

## 视觉依据

- 用户选定图 2：`local/ui-qa/reference.png`。
- 最终桌面：`local/ui-qa/desktop-final.png`。
- 两张图均为 1487 × 1058 像素；浏览器 CSS 视口 1487 × 1058，设备像素比 1，没有缩放或裁剪。
- 状态：`/console/#/servers`、中文、深色主题、选中拥有 53 个工具的服务、概览页、未打开浮层。
- 原图与最终截图在同一次图像输入中按原始分辨率共同查看，核对整体及五项视觉要素。文字、图标和表格在原图尺寸下均可辨读，不需要另行裁剪比较。
- 内容差异：采用隔离演示账号及模拟工具描述；进程编号不复制示意图。服务名称与工具目录在实际应用中均来自 API。发布分类、当前登录身份可见数量与客户端加载分别展示；客户端加载保持“未验证”。这些是依据接口含义作出的内容调整。
- 截图和演示服务位于被 Git 忽略的 `local/`，仅供本地验收；不是生产环境截图或发布资产。

## 比较与修正记录

| 轮次 | 发现与影响 | 修正 | 复核证据 |
| --- | --- | --- | --- |
| 1，P2 | 目录区域偏窄，运行信息和工具文字偏小，状态标记与标题分行，首屏信息比例与原图不同 | 桌面目录改为 356px，正文 14px、分区标题 18px；状态移到标题旁，次要运行字段移到配置页 | `desktop-before.png` → `desktop-after.png`，均与原图共同查看 |
| 2，P2 | 320px 下顶栏将页面挤宽至约 347px；移动导航缺少语言入口 | 最窄宽度收起品牌文字，保留图标与可访问名称；导航抽屉加入语言切换 | `mobile-320-before.png`、`mobile-320-after.png`；最终 320px 视口的页面宽度与滚动宽度均为 320px |
| 3，P2 | 1024px 下服务筛选名称被截断，英文选项仍有少量截断 | 中等宽度目录设为 280px，缩小筛选字号与内边距 | `tablet-before.png` → `tablet-after.png` → `light-en-final.png`；三个英文选项的滚动宽度均等于可用宽度 |
| 4，P2 | 主按钮白字对比度约 3.93:1；浅色链接偏淡；切换语言后页面语言属性未更新 | 主按钮颜色设为 `#246ddd`，白字对比度约 4.88:1，悬停约 4.75:1；浅色链接使用 `#1d62d0`；同步文档语言属性 | `desktop-final.png`、`light-en-final.png`；英文状态的 `html.lang` 为 `en-US` |

表中截图均在 `local/ui-qa/`。最后一次桌面对照已包含以上修正。

## 五项视觉核对

| 项目 | 结果 |
| --- | --- |
| 字体与层级 | 使用本机中文无衬线字体及系统回退；标题、正文、说明分级。长服务名、工具名提供省略与完整名称提示，Schema 可在抽屉查看。Ant Design 的字号和字重保留少量组件风格差异。 |
| 布局与间距 | 保留顶部导航、紧凑侧栏、服务目录、右侧概览两栏和下方工具预览。主区域起点约为 x=440，与原图一致。390px 下改为列表和详情逐层进入，并提供返回入口。 |
| 色彩与状态 | 深灰背景、蓝色选中及操作色、绿色运行状态；深浅主题共享语义。状态包含文字，不仅依赖颜色。主按钮和浅色链接已校正对比度。 |
| 图片与图标 | 复用仓库现有 Gate SVG 标识及组件库图标，没有用临时绘图替代品牌资产。图标清晰；装饰性分区图标不参与标题朗读。 |
| 文案与内容 | 使用实际接口支持的状态，区分发现、分类发布、当前身份可见和客户端加载。配置展示安全响应，健康检查关闭时不暗示调用已验证。中英文文案均已查看。 |

## 浏览器验证

使用仅监听回环地址的隔离演示服务和独立浏览器上下文，没有连接生产写接口。

| 场景 | 观察结果 |
| --- | --- |
| 服务搜索及空结果 | 筛选名称或 ID，未匹配时显示空态；清空搜索可恢复目录 |
| 工具列表与参数 | 切换工具页才请求该服务的 `section=tools`；按名称过滤有效，Schema 抽屉可打开、关闭，并适配 390px 宽度 |
| 日志、事件、恢复 | 打开相应页签后分别请求 `logs`、`events`、`recovery`，显示最近 40 条记录 |
| 配置与缓存 | 配置页请求 `configuration`；展开缓存后才请求 `cache`，概览不触发缓存目录统计 |
| 错误重试 | 在演示浏览器临时模拟一次详情 503，显示错误和重试按钮；重试后恢复工具表格 |
| 快速切换 | 对前一服务的详情响应增加 900ms 延迟后立即切换，迟到响应没有覆盖当前服务标题和数据；验证后移除模拟逻辑 |
| 导航及语言 | 手机导航抽屉可进入服务页面、切换中文；桌面可切换英文、浅色主题；刷新后保留选择，语言属性同步 |
| 键盘 | Ctrl+K 打开搜索，输入“日志”后 Enter 能进入日志页，Escape 可关闭搜索 |
| 窄屏 | 320px、390px 无页面横向溢出；1024px 筛选文字完整。宽表格采用局部滚动 |
| 控制台 | 最终桌面及中英文主题检查未发现浏览器 error 或 warn |

补充截图：`mobile-list-final.png`、`mobile-detail-final.png`、`mobile-schema.png`、`tool-drawer.png`、`detail-error.png`、`service-empty.png`、`command-palette.png`。

## 性能与代码验证

本机 SQLite、已发布目录、同一身份、三次运行的中位数；比较本次修改前的 HEAD 与工作区实现。该结果不包含网络、客户端启动或真实下游服务耗时。

| 工具数 | 之前 | 之后 | 数据库连接数 | SELECT 次数 |
| --- | --- | --- | --- | --- |
| 105 | 275.03ms | 5.05ms | 421 → 1 | 421 → 3 |
| 600 | 2202.58ms | 17.44ms | 2401 → 1 | 2401 → 7 |

原始结果：`local/performance-results.json`。分类与权限仅在请求内批量读取；没有跨用户长期缓存。调用与发现共享授权判定，保留撤权、过期、禁用角色及工具覆盖服务授权的规则。

- 后端适用回归：383 passed、2 skipped、2 deselected。
- 未纳入的两项用例依赖 Linux 可执行位：容器运行器测试及 Linux 原生压缩包测试，Windows 上不能等同验收。
- Python lint 通过；按 Linux 平台进行的 mypy 检查通过。默认 Windows mypy 仍有既有进程控制代码的三个 POSIX 属性错误，本轮未修改相关实现。
- 前端类型检查、UX 契约、16 项测试及完整构建通过。
- 身份检查通过：对 Git 跟踪文件、未忽略的新文件及最终前端构建产物组成的源码快照执行原检查器。工作区内演示数据、下载工具和参考图片不属于发布范围；原始全目录扫描会命中这些本地材料，检查器没有被修改或放宽。
- `git diff --check` 通过。新增 UI 依赖的许可证清单测试通过；两个未附带独立许可证文件的依赖使用仓库既有的许可证补充目录，并记录上游来源。
- 构建仍提示入口脚本约 803KB（gzip 约 266KB）超过建议阈值；未提高阈值或隐去告警。
- 详情默认完整响应保留兼容；新增分区参数及权限边界有 API 测试覆盖。初始页面仍会读取现有当前身份工具目录；按需分区优化针对服务详情的日志、缓存与诊断读取。

## 验收边界与后续

- 本次为本地实现和隔离数据浏览器验收。尚未部署，不能据此确认真实客户端漏发现的问题已经消失。
- 没有对所有历史业务页面进行视觉重做或全量端到端验收；后续可按本样板逐页迁移。
- 余下 P3：可继续细调小字号和列表行距，使其他页面逐步统一；无需阻断本批样板。

## 实施清单

同步远端 `main` 的 `99f7e22` 后，保留双方发布说明并采用远端的 `0.1.2` 版本源；前端类型检查和完整构建、Python lint 再次通过。发布相关回归为 68 passed、5 skipped、1 deselected，排除项仍为 Linux 原生归档可执行位用例。首次运行受默认临时目录权限限制，改用项目内独立临时目录后通过。上面的视觉截图记录首批 UI 验收，版本标识早于本次远端同步。

- [x] 组件库、统一主题和公共导航接入。
- [x] 服务目录、概览、工具、日志与配置完成首批迁移。
- [x] 请求内批量权限读取、连接释放和详情分区加载。
- [x] 修复本次发现的 P0/P1/P2 问题并重新截图比较。
- [x] 记录测试结果、性能范围和未验证项。

final result: passed

## 2026-09-24：MCP 配置编辑器改版

本节仅记录配置编辑器，不改变上面的历史验收结论。

- 已实现：全屏编辑、左侧滚动定位、右侧连续配置、点击说明图标、右上角 JSON 切换、固定保存栏；表单与 JSON 共用草稿，修改保留未识别字段。
- 布局目标：1920×1080 与 2560×1440。使用剩余视口高度滚动，正文宽度自适应；2K 不按比例放大控件。
- 本地验收夹具：`web/test-fixtures/mcp-config-editor.html`，使用真实编辑组件与内存接口桩，不操作真实 MCP 配置。
- 浏览器已验证：1080p 与 2K 没有页面横向溢出；2K 固定操作栏位于视口底部。JSON 非法时不能保存，修正并返回表单后修改保留；帮助弹层可用 Escape 关闭且不关闭编辑器；地址错误可修正；模拟保存保留凭据引用及嵌套 false/0。
- 自动检查：配置模型、草稿与配置页共 12 项测试通过；UX 契约检查通过；本次组件与夹具的定向 TypeScript 检查通过。
- 全项目类型检查受 `App.tsx` 向 DashboardPage 传入尚未声明的 `healthLoaded` 属性阻塞；该文件不是本次修改范围。
- 最终截图复核未完成：内置浏览器大视口截图异常后断连；用户授权切换 Chrome DevTools 后，该工具也返回 `Transport closed`。`web/test-fixtures/evidence/config-2k.png` 是异常捕获产物，不可作为视觉验收证据。最后一次锚点坐标修正尚未在浏览器复核。
- 未执行完整构建、部署或真实后端保存；外部 HTTP 会话自动重连没有在本次实现。

本次配置编辑器 final result: blocked（最终视觉对照与锚点修正后的浏览器复核受工具连接故障阻塞）。
